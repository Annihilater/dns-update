# 阿里云 DNS 配置
aliyun:
  # 访问凭证
  access_key_id: ${ACCESS_KEY_ID}
  access_key_secret: ${ACCESS_KEY_SECRET}
  # 区域设置
  region_id: cn-hangzhou

# 日志配置
logging:
  level: info
  format: json
  output: stdout

server:
  port: ${PORT}
